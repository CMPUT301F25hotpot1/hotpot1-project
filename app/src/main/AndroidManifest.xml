<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CAMERA" />

    <uses-feature
        android:name="android.hardware.camera.any"
        android:required="false" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Lottary"
        tools:targetApi="31">

        <!-- Browse / Notifications / Profile-->
        <activity android:name="com.example.lottary.ui.browse.EventDetailsActivity" />
        <activity android:name="com.example.lottary.ui.browse.QrScanActivity" />
        <activity android:name="com.example.lottary.ui.notifications.NotificationsActivity" />
        <activity android:name="com.example.lottary.ui.browse.BrowseActivity" />
        <activity android:name="com.example.lottary.ui.profile.ProfileActivity" />
        <activity android:name="com.example.lottary.ui.profile.CreateProfileActivity" />
        <activity android:name="com.example.lottary.ui.profile.EditProfileActivity" />

        <!-- Launcher -->
        <activity
            android:name=".ui.events.MyEventsActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Misc -->
        <activity android:name=".MainActivity" />
        <activity android:name=".ui.events.create.CreateEventActivity" />
        <activity android:name=".ui.events.edit.EditEventActivity" />

        <!-- Manage Event flow -->
        <activity android:name=".ui.events.manage.ManageEventActivity" />
        <activity android:name=".ui.events.manage.SendNotificationsActivity" />
        <activity android:name=".ui.events.manage.QrCodeActivity" />
        <activity android:name=".ui.events.manage.MapActivity" />

    </application>
</manifest>
